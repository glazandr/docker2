pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                echo 'jr'
                sh 'ls -l'
                echo 'fkefkle'
            }
        }
    }
}
